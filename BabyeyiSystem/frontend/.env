# API origin only (no /api suffix). Restart Vite after changing.
VITE_API_URL=http://localhost:5100

# Pro UI = babyeyipro/Frontend/web (unified portals). Open path /manager for School Manager, /dos, /teacher, etc.
# Staging/production: full URL, e.g. https://pro.school.example.com — must share API cookie domain rules with VITE_API_URL.
VITE_PRO_APP_URL=http://localhost:5174

# Production example (uncomment and adjust):
# VITE_API_URL=https://babyeyi.rw
# VITE_UPLOADS_BASE=https://babyeyi.rw
