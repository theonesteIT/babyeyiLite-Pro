import { TDistrict, TProvince } from '../types';

type sector_params = {
    province: TProvince;
    district: TDistrict;
};
export declare function Sectors(params?: sector_params): string[] | undefined;
export {};
