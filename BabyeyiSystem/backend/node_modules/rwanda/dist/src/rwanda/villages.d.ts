import { TDistrict, TProvince } from '../types';

type VillageParams = {
    province: TProvince;
    district: TDistrict;
    sector: string;
    cell: string;
};
export declare function Villages(params?: VillageParams): string[] | undefined;
export {};
