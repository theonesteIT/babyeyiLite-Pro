import { TDistrict, TProvince } from '../types';

type CellParams = {
    province: TProvince;
    district: TDistrict;
    sector: string;
};
export declare function Cells(params?: CellParams): string[] | undefined;
export {};
