import { TProvince } from '../types';

type district_params = {
    provinces: TProvince | TProvince[];
};
export declare function Districts(params?: district_params): string[] | undefined;
export {};
