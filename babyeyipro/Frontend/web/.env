# Single dev server for all portals — same API as Babyeyi master backend
VITE_API_URL=http://localhost:5100
# Babyeyi Lite login (email + password + school code). Unauthenticated users are sent here.
VITE_BABYEYI_LOGIN_URL=http://localhost:5173/login
# Vite port (optional). Default 5174.
VITE_DEV_PORT=5174
